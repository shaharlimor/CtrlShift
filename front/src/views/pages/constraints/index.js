import { Typography } from '@mui/material';

import MainCard from '../../../components/layout/MainCard';

const Constraints = () => (
    <MainCard title="Constraints">
        <Typography variant="body2">Insert Constraints page</Typography>
    </MainCard>
);

export default Constraints;
