import { Typography } from '@mui/material';

import MainCard from '../../../components/layout/MainCard';

const ShiftsBoard = () => (
    <MainCard title="Shifts Board">
        <Typography variant="body2">Shifts Board page</Typography>
    </MainCard>
);

export default ShiftsBoard;
